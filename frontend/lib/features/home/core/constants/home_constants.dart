import 'package:flutter/material.dart';

class HomeConstants {
  static const defaultGradient = [
    Color(0xFF091428),
    Color(0xFF0A323C),
    Color(0xFF463714),
  ]; // Gold accent hint (bem sutil no fim)
  static const screenPadding = EdgeInsets.all(16.0);
  static const sectionSpacing = SizedBox(height: 24);
  static const appBarTitle = 'League Music Player';
}
