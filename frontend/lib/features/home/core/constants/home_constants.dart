import 'package:flutter/material.dart';

class HomeConstants {
  static const defaultGradient = [Color(0xFF1F1C2C), Color(0xFF928DAB)];
  static const screenPadding = EdgeInsets.all(16.0);
  static const sectionSpacing = SizedBox(height: 24);
  static const appBarTitle = 'Riot Music';
}
